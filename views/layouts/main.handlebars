<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link
        rel="stylesheet"
        href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
        integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf"
        crossorigin="anonymous"
    />
    <link
        href="https://fonts.googleapis.com/css?family=Open+Sans&display=swap"
        rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
    <header class="head">
      <h1>
        <a href="/">Tech Blog </a><i class="fas fa-apple-alt"></i>
      </h1>
      <nav class="nav">
        {{#if loggedIn}}

        <a href="/" class="btn-no-style">Homepage <i class="fas fa-home"></i></a>
        <a href="/dashboard" class="btn-no-style">Dashboard <i class="fas fa-columns"></i></a>
        <button id="logout" class="btn-no-style">Logout <i class="fas fa-sign-out-alt"></i></button>

        {{else}}
        <a href="/" class="btn-no-style">Homepage <i class="fas fa-home"></i></a>
        <a href="/login" class="btn-no-style">Dashboard <i class="fas fa-columns"></i></a>
        <a href="/login" class="btn-no-style">Login <i class="fas fa-sign-in-alt"></i></a>

        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Welcome!
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</body>
</html>